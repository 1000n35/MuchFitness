{% extends 'base.html.twig' %}

{% block title %}
	Inscription
{% endblock %}

{% block body %}
	{% for message in app.flashes("success") %}
		<div class="flash-notice">
			{{ message }}
		</div>
	{% endfor %}

	{{ form_start(form) }}
	{{ form_row(form) }}
	<button type="sublmit">S'inscrire</button>
	{{ form_end(form) }}
{% endblock %}


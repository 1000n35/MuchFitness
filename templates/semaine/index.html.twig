{% extends 'base.html.twig' %}

{% block title %}Semaine index{% endblock %}

{% block body %}
    <h1>Semaine index</h1>

    <table class="table">
        <thead>
            <tr>
                <th>Id</th>
                <th>DateDebut</th>
                <th>actions</th>
            </tr>
        </thead>
        <tbody>
        {% for semaine in semaines %}
            <tr>
                <td>{{ semaine.id }}</td>
                <td>{{ semaine.dateDebut ? semaine.dateDebut|date('Y-m-d') : '' }}</td>
                <td>
                    <a href="{{ path('app_semaine_show', {'id': semaine.id}) }}">show</a>
                    <a href="{{ path('app_semaine_edit', {'id': semaine.id}) }}">edit</a>
                </td>
            </tr>
        {% else %}
            <tr>
                <td colspan="3">no records found</td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

    <a href="{{ path('app_semaine_new') }}">Create new</a>
{% endblock %}

{% extends 'base.html.twig' %}

{% block title %}Programme{% endblock %}

{% block body %}
    <h1>Programme</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ programme.id }}</td>
            </tr>
            <tr>
                <th>Libelle</th>
                <td>{{ programme.libelle }}</td>
            </tr>
            <tr>
                <th>Description</th>
                <td>{{ programme.description }}</td>
            </tr>
            <tr>
                <th>Type</th>
                <td>{{ programme.type }}</td>
            </tr>
        </tbody>
    </table>


    {#changer le href pour mes suivis programme#}
    
    <a href = "{{ path('app_programme_suivreProgramme', {'id': programme.id}) }}">Suivre le programme</a href> 



    {#changer le href et le if#}
    {% if app.user.getId() == programme.getCreateur.getId()%} {#si user suis le programme A COMPLETER#}
        <a href="/contact" class="btn btn-primary btn-lg px-4 gap-3">Contacter le coach</a>   
    {% endif %}



    <a href="{{ path('app_programme_index') }}">retour à la liste</a>
    {% if app.user.getId() == programme.getCreateur().getId()%}
    <a href="{{ path('app_programme_edit', {'id': programme.id}) }}">edit</a>
   
    {{ include('programme/_delete_form.html.twig') }}
    {% endif %}
{% endblock %}

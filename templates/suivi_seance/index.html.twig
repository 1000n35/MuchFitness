{% extends 'base.html.twig' %}

{% block title %}SuiviSeance index{% endblock %}

{% block body %}
    <h1>SuiviSeance index</h1>

    <table class="table">
        <thead>
            <tr>
                <th>Id</th>
                <th>JourSeance</th>
                <th>Descriptif</th>
                <th>actions</th>
            </tr>
        </thead>
        <tbody>
        {% for suivi_seance in suivi_seances %}
            <tr>
                <td>{{ suivi_seance.id }}</td>
                <td>{{ suivi_seance.jourSeance }}</td>
                <td>{{ suivi_seance.descriptif }}</td>
                <td>
                    <a href="{{ path('app_suivi_seance_show', {'id': suivi_seance.id}) }}">show</a>
                    <a href="{{ path('app_suivi_seance_edit', {'id': suivi_seance.id}) }}">edit</a>
                </td>
            </tr>
        {% else %}
            <tr>
                <td colspan="4">no records found</td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

    <a href="{{ path('app_suivi_seance_new') }}">Create new</a>
{% endblock %}

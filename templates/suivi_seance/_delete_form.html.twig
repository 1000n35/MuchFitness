<form method="post" action="{{ path('app_suivi_seance_delete', {'id': suivi_seance.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ suivi_seance.id) }}">
    <button class="btn">Delete</button>
</form>

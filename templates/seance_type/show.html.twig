{% extends 'base.html.twig' %}

{% block title %}SeanceType{% endblock %}

{% block body %}
    <h1>SeanceType</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ seance_type.id }}</td>
            </tr>
            <tr>
                <th>Jour</th>
                <td>{{ seance_type.jour }}</td>
            </tr>
            <tr>
                <th>Libelle</th>
                <td>{{ seance_type.libelle }}</td>
            </tr>
            <tr>
                <th>Split</th>
                <td>{{ seance_type.split }}</td>
            </tr>
            <tr>
                <th>Descriptif</th>
                <td>{{ seance_type.descriptif }}</td>
            </tr>
            <tr>
                <th>Duree</th>
                <td>{{ seance_type.duree ? seance_type.duree|date('H:i:s') : '' }}</td>
            </tr>
        </tbody>
    </table>


    <h2>Exercices de la séance</h2>
        <table class="table">
            <tbody>
                <tr>
                    <th>Nom de l'exercice</th>
                    <th>Descriptif</th>
                    <th>Video</th>
                    <th>Actions</th>
                </tr>
                {% for exercice in seance_type.exercices %}
                    <tr>
                        <td>{{ exercice.nomExercice }}</td>
                        <td>{{ exercice.descriptif }}</td>
                        <td><a href="{{ asset('uploads/video/' ~ exercice.videoFilename) }}">Voir la vidéo</a></td>
                        <td>
                        <a href = "{{ path('app_exercice_show', {'id': exercice.id, 'seancetypeid' : seance_type.id}) }}">Détail de l'exerice</a href>
                        </td>
                    </tr>
                {% endfor %}

            </tbody>
        </table>
        <a href = "{{ path('app_programme_show', {'id': seance_type.programme.id}) }}">Retour au programme</a href>
{% endblock %}
